# Mood 列表页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新加 `MoodListPage` widget (1 page = 1 directory), 时间倒序列表 + filter (date / score / cbt level) + search note + sort 切换

**Architecture:** 1 个 filter state provider (Notifier) + 1 个 filter pipeline provider + 1 page widget (filter chips + search + sort + ListView) + 1 home page 入口

**Tech Stack:** Flutter 3.41.9 / Riverpod 3.x / go_router 14.6

## Global Constraints

- Flutter 3.41.9 / Dart 3.12.2
- 4-layer architecture
- 守门员: `flutter analyze` 0, `flutter test` 全过, 16 脚本全绿
- TDD: red → green → commit
- baseline 1472 pass / 0 fail (R86 后)

## File Structure

### 新增
- `lib/presentation/pages/mood_list/mood_list_page.dart`
- `lib/presentation/pages/mood_list/widgets/mood_list_item.dart`
- `lib/presentation/pages/mood_list/widgets/mood_list_filter_bar.dart`
- `lib/presentation/providers/mood_list_filter_provider.dart`
- `test/presentation/providers/mood_list_filter_provider_round87_test.dart`
- `test/presentation/pages/mood_list/mood_list_page_round87_test.dart`
- `test/presentation/pages/mood_list/mood_list_item_round87_test.dart`

### 修改
- `lib/presentation/pages/home/home_page.dart` (加 "查看所有 mood" 按钮)
- `lib/core/routing/app_router.dart` (加 `/mood-list` 路由)
- `lib/l10n/app_zh.arb` / `app_en.arb` / `app_zh_Hant.arb` (~10 keys)

---

### Task 1: 数据层 (filter + search + sort)

**Files:**
- Create: `lib/presentation/providers/mood_list_filter_provider.dart`
- Test: `test/presentation/providers/mood_list_filter_provider_round87_test.dart`

**Interfaces:**
- Produces:
  - `class MoodListFilter` (immutable) — dateRange / minScore / maxScore / cbtLevel / searchQuery / sort
  - `enum MoodListSort { timestampDesc, scoreAsc, scoreDesc }`
  - `moodListFilterProvider` (NotifierProvider<MoodListFilter>)
  - `filteredMoodEntriesProvider` (Provider<List<MoodEntryEntity>>) — applies filter + search + sort

- [ ] **Step 1: 写失败测试**

`test/presentation/providers/mood_list_filter_provider_round87_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:chroniccare/presentation/providers/mood_list_filter_provider.dart';
import 'package:chroniccare/domain/entities/mood_entry_entity.dart';

void main() {
  // helper: build 5 mood entries with varied score / cbtLevel / note
  List<MoodEntryEntity> makeEntries() => [
    MoodEntryEntity(id: 1, timestamp: DateTime(2026, 8, 5), score: 5, note: '开心'),
    MoodEntryEntity(id: 2, timestamp: DateTime(2026, 8, 4), score: 2, note: '难受'),
    MoodEntryEntity(id: 3, timestamp: DateTime(2026, 8, 3), score: 3, note: '一般',
      situation: 's', automaticThought: 'at', evidenceFor: 'ef', evidenceAgainst: 'ea',
      alternativeThought: 'alt', reratedScore: 4,  // cbtLevel = 5
    ),
    MoodEntryEntity(id: 4, timestamp: DateTime(2026, 8, 2), score: 4, note: 'OK'),
    MoodEntryEntity(id: 5, timestamp: DateTime(2026, 8, 1), score: 1, note: '差',
      situation: 's', automaticThought: 'at', evidenceFor: 'ef', evidenceAgainst: 'ea',
      alternativeThought: 'alt', reratedScore: 3, coreBelief: 'cb', behaviorResponse: 'br',  // cbtLevel = 7
    ),
  ];

  group('filteredMoodEntriesProvider (v0.30 round 87)', () {
    test('默认无 filter: 5 条全部, 时间倒序', () {
      final container = ProviderContainer(overrides: [
        // TODO: 找现有 moodEntriesProvider 名字 (跟 R85 task 1 一样, 用 sync wrapper)
      ]);
      addTearDown(container.dispose);
      final result = container.read(filteredMoodEntriesProvider);
      expect(result.length, 5);
      expect(result[0].id, 1);  // 最新在前面
    });

    test('search "难" 过滤 → 1 条 (id=2)', () {
      // override filter state, then check
    });

    test('cbtLevel=5 filter → 1 条 (id=3)', () {
      // ...
    });

    test('sort score asc → 5 条 score 1, 2, 3, 4, 5', () {
      // ...
    });
  });
}
```

- [ ] **Step 2: 跑测试验证失败**

```bash
flutter test test/presentation/providers/mood_list_filter_provider_round87_test.dart
```

Expected: FAIL — provider 不存在.

- [ ] **Step 3: 实现 provider**

`lib/presentation/providers/mood_list_filter_provider.dart`:

```dart
// v0.30 round 87 (sub-spec 3 mood 列表页): filter + search + sort state
//
// 复用 R85 task 1 的 cbt_rerated_entries_provider.dart 里加的 moodEntriesProvider wrapper
// (如果 R86 移到 shared_providers.dart, 这里 import 它)

import 'package:flutter/material.dart';  // for DateTimeRange
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:chroniccare/domain/entities/mood_entry_entity.dart';
// import '...mood_entries_provider.dart'  // TODO: 找实际路径

enum MoodListSort { timestampDesc, scoreAsc, scoreDesc }

class MoodListFilter {
  final DateTimeRange? dateRange;
  final int? minScore;
  final int? maxScore;
  final int? cbtLevel;  // null = all
  final String searchQuery;
  final MoodListSort sort;

  const MoodListFilter({
    this.dateRange,
    this.minScore,
    this.maxScore,
    this.cbtLevel,
    this.searchQuery = '',
    this.sort = MoodListSort.timestampDesc,
  });

  MoodListFilter copyWith({...}) {...}
}

class MoodListFilterNotifier extends Notifier<MoodListFilter> {
  @override
  MoodListFilter build() => const MoodListFilter();

  void setSearchQuery(String q) => state = state.copyWith(searchQuery: q);
  void setDateRange(DateTimeRange? r) => state = state.copyWith(dateRange: r);
  void setMinScore(int? s) => state = state.copyWith(minScore: s);
  void setMaxScore(int? s) => state = state.copyWith(maxScore: s);
  void setCbtLevel(int? l) => state = state.copyWith(cbtLevel: l);
  void setSort(MoodListSort s) => state = state.copyWith(sort: s);
  void reset() => state = const MoodListFilter();
}

final moodListFilterProvider = NotifierProvider<MoodListFilterNotifier, MoodListFilter>(
  MoodListFilterNotifier.new,
);

/// 过滤 + 搜索 + 排序 后的 list
final filteredMoodEntriesProvider = Provider<List<MoodEntryEntity>>((ref) {
  final all = ref.watch(moodEntriesProvider);
  final filter = ref.watch(moodListFilterProvider);

  Iterable<MoodEntryEntity> result = all;

  if (filter.dateRange != null) {
    result = result.where((e) =>
      !e.timestamp.isBefore(filter.dateRange!.start) &&
      !e.timestamp.isAfter(filter.dateRange!.end));
  }
  if (filter.minScore != null) {
    result = result.where((e) => e.score >= filter.minScore!);
  }
  if (filter.maxScore != null) {
    result = result.where((e) => e.score <= filter.maxScore!);
  }
  if (filter.cbtLevel != null) {
    result = result.where((e) => e.cbtLevel == filter.cbtLevel);
  }
  if (filter.searchQuery.isNotEmpty) {
    final q = filter.searchQuery.toLowerCase();
    result = result.where((e) =>
      (e.note ?? '').toLowerCase().contains(q) ||
      (e.tagsJson).toLowerCase().contains(q));  // 简化: 也搜 tagsJson 字符串
  }

  final list = result.toList();
  switch (filter.sort) {
    case MoodListSort.timestampDesc:
      list.sort((a, b) => b.timestamp.compareTo(a.timestamp));
    case MoodListSort.scoreAsc:
      list.sort((a, b) => a.score.compareTo(b.score));
    case MoodListSort.scoreDesc:
      list.sort((a, b) => b.score.compareTo(a.score));
  }
  return list;
});
```

- [ ] **Step 4: 跑测试验证通过**

```bash
flutter test test/presentation/providers/mood_list_filter_provider_round87_test.dart
```

Expected: PASS 4/4.

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/providers/mood_list_filter_provider.dart test/presentation/providers/mood_list_filter_provider_round87_test.dart
git commit -m 'v0.30 round 87 (data): moodListFilterProvider + filteredMoodEntriesProvider (filter + search + sort)'
```

---

### Task 2: MoodListItem widget

**Files:**
- Create: `lib/presentation/pages/mood_list/widgets/mood_list_item.dart`
- Test: `test/presentation/pages/mood_list/mood_list_item_round87_test.dart`

**Interfaces:**
- Produces: `MoodListItem({required MoodEntryEntity entry})` — 单行渲染 (timestamp + score emoji + note preview + CBT badge)

- [ ] **Step 1: 写失败测试**

`test/presentation/pages/mood_list/mood_list_item_round87_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:chroniccare/l10n/app_localizations.dart';
import 'package:chroniccare/presentation/pages/mood_list/widgets/mood_list_item.dart';
import 'package:chroniccare/domain/entities/mood_entry_entity.dart';

void main() {
  Widget wrap(Widget child) => MaterialApp(
    localizationsDelegates: AppLocalizations.localizationsDelegates,
    supportedLocales: AppLocalizations.supportedLocales,
    locale: const Locale('zh'),
    home: Scaffold(body: child),
  );

  testWidgets('3-栏 entry: 显示 timestamp + score emoji + note', (tester) async {
    final entry = MoodEntryEntity(
      id: 1, timestamp: DateTime(2026, 8, 4, 14, 32),
      score: 4, note: '开会迟到',
    );
    await tester.pumpWidget(wrap(MoodListItem(entry: entry)));
    expect(find.text('开会迟到'), findsOneWidget);
    expect(find.textContaining('14:32'), findsOneWidget);
  });

  testWidgets('5-栏 entry: 显示 CBT 5 栏 badge', (tester) async {
    final entry = MoodEntryEntity(
      id: 2, timestamp: DateTime(2026, 8, 4),
      score: 3, situation: 's', automaticThought: 'at',
      evidenceFor: 'ef', evidenceAgainst: 'ea',
      alternativeThought: 'alt', reratedScore: 4,
    );
    await tester.pumpWidget(wrap(MoodListItem(entry: entry)));
    expect(find.text('CBT 5 栏'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 跑测试验证失败**

```bash
flutter test test/presentation/pages/mood_list/mood_list_item_round87_test.dart
```

Expected: FAIL — `MoodListItem` 不存在.

- [ ] **Step 3: 实现 widget**

`lib/presentation/pages/mood_list/widgets/mood_list_item.dart`:

```dart
// v0.30 round 87 (sub-spec 3 mood 列表页): 单行 entry 渲染
//
// 跟 trend_calendar DayDetailCard 复用 MoodVisual + AppTokens

import 'package:flutter/material.dart';
import 'package:chroniccare/core/shared/mood_visual.dart';
import 'package:chroniccare/core/theme/app_tokens.dart';
import 'package:chroniccare/domain/entities/mood_entry_entity.dart';
import 'package:chroniccare/l10n/app_localizations.dart';

class MoodListItem extends StatelessWidget {
  final MoodEntryEntity entry;
  final VoidCallback? onTap;
  const MoodListItem({super.key, required this.entry, this.onTap});

  @override
  Widget build(BuildContext context) {
    final visual = MoodVisual.of(entry.score);
    final timestamp = '${entry.timestamp.month.toString().padLeft(2, '0')}-${entry.timestamp.day.toString().padLeft(2, '0')} ${entry.timestamp.hour.toString().padLeft(2, '0')}:${entry.timestamp.minute.toString().padLeft(2, '0')}';
    final preview = entry.note?.isNotEmpty == true
        ? entry.note!
        : entry.automaticThought?.isNotEmpty == true
            ? entry.automaticThought!
            : '...';

    return ListTile(
      onTap: onTap,
      leading: Text(visual.emoji, style: const TextStyle(fontSize: 28)),
      title: Text(timestamp, style: AppTokens.textStyleCaption(context)),
      subtitle: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text('${entry.score}/5  $preview', maxLines: 1, overflow: TextOverflow.ellipsis),
          if (entry.isCbtRecord) Padding(
            padding: const EdgeInsets.only(top: AppTokens.spacingXxs),
            child: Container(
              padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
              decoration: BoxDecoration(
                color: AppTokens.tintedPrimaryDeep(context),
                borderRadius: BorderRadius.circular(AppTokens.radiusChip),
              ),
              child: Text(
                entry.cbtLevel == 7 ? 'CBT 7 栏' : 'CBT 5 栏',
                style: AppTokens.textStyleMicro(context).copyWith(color: AppTokens.primaryColor(context)),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: 跑测试验证通过**

```bash
flutter test test/presentation/pages/mood_list/mood_list_item_round87_test.dart
```

Expected: PASS 2/2.

- [ ] **Step 5: Commit**

```bash
git add lib/presentation/pages/mood_list/widgets/mood_list_item.dart test/presentation/pages/mood_list/mood_list_item_round87_test.dart
git commit -m 'v0.30 round 87 (ui): MoodListItem 单行 entry 渲染 (timestamp + score + note + CBT badge)'
```

---

### Task 3: MoodListFilterBar widget (filter chips + sort dropdown)

**Files:**
- Create: `lib/presentation/pages/mood_list/widgets/mood_list_filter_bar.dart`
- Test: `test/presentation/pages/mood_list/mood_list_filter_bar_round87_test.dart`

**Interfaces:**
- Produces: `MoodListFilterBar` — Row of filter chips + sort dropdown

- [ ] **Step 1: 写失败测试**

```dart
testWidgets('filter bar 显示 3 chips + sort', (tester) async {
  // pump MoodListFilterBar, verify 3 chips (日期/分数/CBT) + 1 sort dropdown
});
```

- [ ] **Step 2: 跑测试验证失败**

- [ ] **Step 3: 实现 widget** (跟 R85 task 2 同模式, ActionChip + showModalBottomSheet 选 date range / score / cbt level)

- [ ] **Step 4: 跑测试验证通过**

- [ ] **Step 5: Commit**

```bash
git commit -m 'v0.30 round 87 (ui): MoodListFilterBar filter chips + sort dropdown'
```

---

### Task 4: MoodListPage (orchestrator) + home page 入口 + go_router 路由

**Files:**
- Create: `lib/presentation/pages/mood_list/mood_list_page.dart`
- Modify: `lib/presentation/pages/home/home_page.dart` (加 "查看所有" 按钮)
- Modify: `lib/core/routing/app_router.dart` (加 `/mood-list` 路由)

- [ ] **Step 1: 写失败测试**

`test/presentation/pages/mood_list/mood_list_page_round87_test.dart`:

```dart
testWidgets('MoodListPage 显示 search + filter bar + ListView', (tester) async {
  // pump MoodListPage with ProviderScope overrides
  // verify search TextField + 3 filter chips + ListView (or empty state)
});

testWidgets('search "难" 实时过滤', (tester) async {
  // enterText find.byType(TextField), pump, verify list 更新
});
```

- [ ] **Step 2-5: TDD, 跑测试, commit**

- [ ] **Step 6: home page 加按钮 + router 路由**

`lib/presentation/pages/home/home_page.dart`: 在 MoodQuickButton 附近加 "📋 Mood 历史" 按钮 → `context.push('/mood-list')`

`lib/core/routing/app_router.dart`: 加 GoRoute(path: '/mood-list', builder: (ctx, state) => const MoodListPage())

- [ ] **Step 7: Commit**

```bash
git add lib/presentation/pages/mood_list/mood_list_page.dart \
        lib/presentation/pages/home/home_page.dart \
        lib/core/routing/app_router.dart \
        test/presentation/pages/mood_list/mood_list_page_round87_test.dart
git commit -m 'v0.30 round 87 (page): MoodListPage + home 入口 + /mood-list 路由'
```

---

### Task 5: i18n + CHANGELOG + final review

**Files:**
- Modify: 3 ARB files (10 keys)
- Modify: `docs/CHANGELOG.md`

- [ ] **Step 1: 加 10 keys (zh)**

`lib/l10n/app_zh.arb` 末尾加:

```json
,
  "moodListPageTitle": "Mood 历史",
  "moodListSearchHint": "搜索 note...",
  "moodListFilterDate": "日期",
  "moodListFilterScore": "分数",
  "moodListFilterCbt": "CBT 档位",
  "moodListSortBy": "排序",
  "moodListSortTimestamp": "时间倒序",
  "moodListSortScoreAsc": "分数升序",
  "moodListSortScoreDesc": "分数降序",
  "moodListEmpty": "还没有 mood 记录",
  "moodListNoMatch": "没有匹配的记录",
  "moodListEntryCount": "{count} 条记录"
```

- [ ] **Step 2: 加 10 keys (en + zh_Hant)**

(类似 en/zh_Hant 翻译)

- [ ] **Step 3: flutter gen-l10n**

- [ ] **Step 4: 守门员 + analyze + test**

```bash
python scripts/check_arb_keys.py
python scripts/check_orphan_arb_keys.py
flutter analyze
flutter test
```

Expected: 0 error, 1472 + 6-8 new tests = 1478-1480 pass

- [ ] **Step 5: CHANGELOG [0.30.0] R87 entry**

- [ ] **Step 6: Commit final**

```bash
git commit -m 'v0.30 round 87 (i18n): 12 ARB keys + CHANGELOG + mood 列表页 spec/plan'
```

---

## Self-Review

- [x] Spec coverage: data (Task 1) / MoodListItem (Task 2) / FilterBar (Task 3) / Page + 入口 (Task 4) / i18n (Task 5)
- [x] No placeholders
- [x] Type consistency: MoodListFilter immutable + copyWith, filter pipeline pure function
- [x] TDD: red → green → commit per task
- [x] DRY: 复用 MoodVisual + AppTokens + cbtLevel getter
- [x] YAGNI: 不做 edit/delete/bulk/export (留 v0.31)
