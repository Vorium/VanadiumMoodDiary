/// ⚠️ DEPRECATED — moved to `lib/shared/formatters.dart` (v0.16 round 19)
///
/// 这个文件保留只是为了不破坏历史 import 路径。新代码请直接 import `shared/formatters.dart`。
/// v0.17+ 会物理删除。
@Deprecated('Use lib/shared/formatters.dart instead')
library;

export '../../shared/formatters.dart';
