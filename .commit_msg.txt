v0.14 round 16: lint cleanup - 0 errors, 0 warnings, 184 info (trailing comma)

Removed unused imports and one unused local variable left over
from earlier rounds. Most are in files I touched during round
14 (assessment_history, medications_list_widget, refill_manage,
medication_calendar) but a few are pre-existing in untouched
files (app_database, medication_mapper, notification_payload,
settings_page, edit_medication_dialog, 3 test files).

Also simplified notification_payload.parse: uri.host is
non-nullable, the action == null branch was dead code.

13 files changed, 21 lines removed (and 2 added for the
notification_payload simplification).
