v0.25 round 56d: 杂项清理 (spen P0 #15) — formatters 走 intl + vent_detail EmptyState

2 处 cleanup:

1. lib/core/shared/formatters.dart 走 intl DateFormat
   - 之前手动 _pad + 字符串拼接 ('${d.year}-${_pad(d.month)}-${_pad(d.day)}')
   - 改用 intl package 的 DateFormat:
     * 4 个 static DateFormat 实例 (date / monthDay / time / dateCompact) lazy init
     * 性能: 1 次构造 vs 每次调用 new
     * locale-aware (未来 zh/en 切换不用改这里)
     * 业界标准格式模式 (yyyy-MM-dd / HH:mm)
   - public API 不变: Formatters.date / monthDay / time / dateTime / dateCompact
   - 现有 9 个 caller (medication_report / stat_calculator / report_dialog 等) 不动

2. lib/presentation/pages/vent/vent_detail_page.dart:191 改 EmptyState
   - 之前: Center(child: Text(AppLocalizations.of(context).ventDetailNotFound))
   - 改后: EmptyState(icon: Icons.search_off, title: ventDetailNotFound)
   - 跟 vent_list / assessment_history / medication_calendar / report_history /
     contact_list 等 5+ 处空态页面统一风格 (P1-2 v0.18 round 14 抽的 EmptyState)
   - 加 import 'package:chroniccare/presentation/widgets/empty_state.dart'

verify:
- flutter analyze: 0 error (28 info, prefer_const_constructors)
- flutter test: 1098 全过 (跟 R56c''' 持平, 无功能变化)
- 11 守门员脚本: 全绿
