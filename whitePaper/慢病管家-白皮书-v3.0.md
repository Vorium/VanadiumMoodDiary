# 慢病管家 · 白皮书 v3.0

> **版本**：v3.0 · 2026-07-20
> **作者**：Mavis（root session）
> **保密级别**：团队内部 / 含未公开商业判断，**请勿外传**
> **配套材料**：本文件为总纲；详细规格见 §17 附录
> **合并来源**：`WHITEPAPER.md`（v0.17 round 7 后项目档案）+ `WHITE_PAPER.md`（v0.1 产品白皮书），去重 + 修正过时数据
> **当前基线**：v0.17 round 7 已 commit，703 test cases pass，0 error / 0 warning

---

## 📌 角色快速导航（10 秒找到你的章节）

| 你的角色 | 必看章节 | 阅读时间 | 看后能干嘛 |
|---|---|---|---|
| **产品经理（PM）** | §0, §1, §2, §3, §4, §5, §11, §12, §13 | 30 分钟 | 跟团队对需求、定优先级、砍功能 |
| **Flutter 开发者** | §5, §6, §7, §8, §9, §10, §15, §16 | 45 分钟 | 写代码、跑测试、做 Code Review |
| **UI/UX 设计师** | §1, §2, §7 | 30 分钟 | 出图、做交互、跟开发对接 |
| **测试 / QA** | §8（OEM 自检）, §10, §15（已知坑）| 30 分钟 | 写测试用例、跑回归 |
| **运营 / 市场** | §0, §2, §3, §4 | 20 分钟 | 写宣传文案、选渠道、定价 |
| **投资人 / 老板** | §0, §1, §4, §13 | 15 分钟 | 决定是否继续投 |
| **新接手者** | 先读 §0 文档地图 + §0 执行摘要，再按需跳读 | - | - |

**速读路径**（5 分钟过完整个项目）：
> §0（执行摘要）→ §1.1（一句话愿景）→ §3.2（5 个护城河）→ §4（商业模式）→ §11（路线图）

---

## §0 · 执行摘要（1 分钟）

**慢病管家**（App 内品牌名"**停药通知**"）是一款**为精神心理疾病患者（焦虑 / 抑郁 / 双相 / 睡眠障碍）做的"吃药打卡 + 停药通知 + 情绪量表"三合一的极简 App**。

- **核心问题**：精神心理患者依从性差（漏吃药率 40-60%），断药 48 小时后情绪可能剧烈波动甚至复发；现有方案要么太重（Medisafe / Pillo 偏慢病管理）、要么太轻（死了么只做失联通知）、要么偏 AI 倾听（点点专家 / 心境奇旅，已红海）。
- **我们的解法**：极简打卡（3 秒）+ 48h 没签到自动通知紧急联系人 + PHQ-9 / GAD-7 量表自评 + **心情红绿灯 × 服药依从性双轴图**（核心差异化）。
- **目标用户**：18-45 岁、双相 / 抑郁 / 焦虑 / 睡眠障碍、怕病耻感、用药种类 1-3 种。
- **当前状态**：v0.17 round 7 已 commit，**703 test cases pass**，0 error / 0 warning，0 Flutter / 0 Drift 跨层违规。
- **商业模式**：8 元付费下载（参考"死了么"定价），无广告、无订阅、无数据售卖。
- **路线图**：v0.5 MVP 上架（已）→ v0.17 完整功能（已）→ v1.0 医生版 + 加密云备份 → v2.0 AI 情绪记忆 + 多端。
- **团队**：单人项目（PM / Flutter Dev / 设计师 外包）。

---

## §1 · 项目愿景

### 1.1 一句话定位

> **"让每一个精神心理患者都能被温柔地提醒按时吃药——不靠意志力，靠系统。"**

### 1.2 三个"不做"原则（保护产品定位）

| 不做 | 原因 |
|---|---|
| ❌ **不做游戏化**（火焰 / 段位 / 积分 / 排行）| 精神心理用户会被"太幼稚"刺眼，引发自我否定 |
| ❌ **不做"死 / 危"类字眼** | 病耻感 + App Store 审核风险（死了么曾被下架）|
| ❌ **不做 AI 倾听 / AI 咨询** | 已红海，被 AI 大厂卷；我们做工具不做对话 |
| ❌ **不做社交**（朋友圈 / 家族群打卡）| 暴露患者身份 + 引发病耻感比较 |
| ❌ **不做云端账号**（强制注册）| 隐私优先 + 精神心理患者怕留痕 |
| ❌ **不做订阅**（一次性买断 8 元）| 中国用户订阅疲劳 + 病耻感 |
| ❌ **不暴露病名**（邮件 / 通知 / 营销）| 隐私 + 病耻感 + 不被邮件过滤 |
| ❌ **不售卖用户数据** | 法律 + 道德 |

### 1.3 我们做的三件事

1. **极简打卡**（3 秒，摩擦最小）→ 让"吃药"变成本能
2. **48h 兜底**（自动 SMS 通知紧急联系人）→ 不怕忘、断了有人接
3. **量表自评**（PHQ-9 / GAD-7 + 趋势图）→ 让用户看见"我的状态"

### 1.4 关键洞察

> **精神心理患者最需要的不是"动力"，是"系统"。** 意志力是稀缺资源（尤其在抑郁 / 焦虑期），系统是稳定资源。我们的产品本质是**给精神心理用户的"自动驾驶"**——他们不用记、不用选、不用想，只要点一下"吃了"就行。

### 1.5 差异化

- **零云端**：所有数据本地 SQLCipher 加密，连不上网也能用
- **失联兜底**：48h 未打卡自动通知紧急联系人（措辞"请你方便时提醒我"，不渲染"快不行了"）
- **私密倾诉（树洞）**：完全独立模块，不进任何分析 / 通知 / 报告
- **心情 × 吃药双轴图**：独家洞察——"情绪低谷 = 漏药高发期"

---

## §2 · 目标用户与痛点

### 2.1 目标用户画像

| 维度 | 描述 |
|---|---|
| **人群** | 精神心理疾病患者（抑郁、焦虑、双相、精神分裂、PTSD）|
| **年龄** | 18-45 岁（中位数 28）|
| **服药状态** | 已确诊 6+ 个月、稳定服药方案 1-3 种 |
| **病程** | 1-10 年（已确诊、已稳定用药）|
| **场景** | 上班族 / 学生 / 自由职业（不区分）|
| **设备** | Android 7+ / iOS 13+（国产中端机为主）|
| **经济** | 月收入 3k-15k（不愿每月订阅；愿一次付 8 元）|
| **心理** | 怕病耻感、怕被家人知道、怕"别人觉得我有病" |

**不服务的人**（明确边界）：
- 急性发作期（需要医生干预，不是工具的事）
- 不愿用 App 的（不强求）

### 2.2 关键痛点

| 痛点 | 严重度 | 现有解法（差）| 我们的解法 |
|---|---|---|---|
| **漏吃药 / 忘吃药** | ⭐⭐⭐⭐⭐ | 闹钟（但用户会关掉 / 忽略）| 推送 + 桌面角标 + SMS 兜底 |
| **断药后情绪失控** | ⭐⭐⭐⭐⭐ | 死扛 | 48h 兜底通知紧急联系人 |
| **不知道"我现在怎么样"** | ⭐⭐⭐⭐ | 量表（但要跑医院）| PHQ-9 / GAD-7 自评 + 趋势图 |
| **复诊时说不清"最近怎么样"** | ⭐⭐⭐ | 凭感觉 | PDF 依从性报告（吃药率 + 情绪曲线）|
| **怕被人知道** | ⭐⭐⭐⭐⭐ | 假装没事 | 纯本地存储 + 邮件不显示病名 + 无社交 |
| **意志力差** | ⭐⭐⭐⭐ | "坚持就好" | 不靠意志力，靠系统（核心信念）|

### 2.3 反方场景（用户**不**用我们 App 的原因）

| 反对意见 | 我们的应对 |
|---|---|
| "我不用提醒，我自己能记住" | 第一次忘吃药前都这么说；提供 1 个月免费试用 |
| "我不想让别人知道" | 默认紧急联系人=不通知；用户完全控制 |
| "我吃的药不在我的脑子里" | 我们的本地加密 + 不上传数据，符合他们的隐私要求 |
| "我吃的是维生素 / 中药 / 保健品" | 我们聚焦精神心理，慢病用户用 Pillo 即可 |

---

## §3 · 竞品分析与差异化

### 3.1 竞品四象限

```
              失联通知（安全）
                   ↑
                   │
        死了么    │    在么在么
        撸了么    │    （多紧急联系人 + 适老化）
                   │
    ───────────────┼────────────────→ 吃药管理
                   │
        Wysa      │    Medisafe
        点点专家  │    Pillo
        心境奇旅  │    MyTherapy
                   │
              心理倾听
```

**我们的位置**（X 标记）：

```
              失联通知
                   ↑
                   │
                   │       X ← 慢病管家
                   │
                   │
                   └────────────────→ 吃药管理
```

**关键洞察**：**"精神心理 + 吃药 + 失联通知"三角交叉是空白市场**。

| 现有产品 | 不做的原因 |
|---|---|
| 死了么 / 在么在么 | 只做失联通知，不做精神心理、不做量表 |
| Medisafe / Pillo | 只做吃药管理，不做精神心理、不做失联通知 |
| 点点专家 / 心境奇旅 / Wysa | 只做 AI 倾听 / 咨询，不做吃药、不做失联 |
| Rosebud / Daylio | 只做情绪日记，不做吃药、不做失联 |

### 3.2 5 个护城河（核心差异化）

1. **"心情 × 吃药"双轴图**（心光 App 启发，独家）
   - 吃每颗药时顺手选 🟢🟡🔴 心情，AI 聚合生成双轴图
   - **发现情绪低谷 = 漏药高发期**——这才是真正的精神心理患者专属洞察
   - 别人没做，因为：Medisafe 不关心情绪、心境奇旅不关心吃药

2. **"复发预警"算法**（基于 Youper 阈值告警思路）
   - 滑动窗口 + 阈值告警：「你今天的情绪比上周低 30%，建议联系医生」
   - **不通知紧急联系人**（避免过度干预），只通知患者本人
   - 跟心境的"AI 焦虑识别"区别：我们聚焦"行为改变"信号

3. **本地优先 + 隐私优先**
   - AES-256 加密 + 不上传数据 + 不强制注册
   - 适合"病耻感极强"的精神心理用户

4. **极简 3 秒打卡**（habit 心理学 + 适老化）
   - 摩擦最小化：用户吃每颗药只用 1 滑 + 1 点
   - 不做社交、不做排行、不做游戏化

5. **PDF 依从性报告给医生**（Medisafe 模式 + 中国本土化）
   - 复诊时给医生看：吃药率 / 情绪曲线 / 量表趋势 / 漏药日期
   - 真实使用场景（不是炫耀数据）

### 3.3 已深度借鉴 / 主动不学

**已深度借鉴**（不再重复调研）：

| 项目 | 借鉴了什么 |
|---|---|
| 死了么 / 在么在么 | 48h 没签到自动通知紧急联系人 + 适老化 |
| Medisafe / Pillo | 多药管理 + PDF 报告 + 依从性统计 |
| Woebot | PHQ-9 / GAD-7 量表 + 同行评审研究背书 |
| 点点专家 / 心境奇旅 | 极简克制 + 隐私优先 + 不做游戏化 |
| 简单心理 / 壹心理 | 危机干预热线直接拨打 |
| Duolingo | 推送时间固定 20:00 + "少 1 次没关系"哲学 |

**主动不学**：

| 项目 | "看起来好"但别学 | 原因 |
|---|---|---|
| Duolingo 火焰 / 段位 / 排行 | 游戏化机制 | 精神心理用户被"太幼稚"刺眼 |
| 简单心理 / 壹心理 | 咨询师平台 | 重运营，个人做不动 |
| 心光 App「水晶球」 | 本地 AI 树洞 | 模型大、需要持续训练，MVP 不做 |
| HookBook 性健康追踪 | 垂直慢病 | 用户群不重合 + 合规风险 |
| Monsenso | 临床 EHR 协作 | 私人项目不做医生端 |

### 3.4 市场规模

| 维度 | 数据 | 来源 |
|---|---|---|
| 国内抑郁症患者 | 9500 万 | 卫健委 2022 |
| 国内焦虑障碍患者 | 1.1 亿 | 卫健委 2022 |
| 精神科医师密度 | 2.2 / 10 万人 | 卫健委 2022（高收入国家 10+）|
| 候诊周期 | 2-4 周 | 三甲医院公开数据 |
| 心理咨询费用 | 300-800 元 / 次 | 行业均价 |
| 死了么下载价 | 8 元（原价 1 元，1 周后调）| App Store 中国区 |
| 在么在么试点 | 杭州上城区南星街道 | 政府合作 |

**TAM / SAM / SOM**：
- **TAM**（潜在市场）：精神心理用药患者约 5000 万（含已确诊 + 长期用药）
- **SAM**（可服务市场）：18-45 岁 + 用 1-3 种药 + 智能手机 = 约 2000 万
- **SOM**（可获取市场）：3 年内能触达 1%（保守估计）= 20 万付费用户

---

## §4 · 商业模式

### 4.1 定价策略

| 项目 | 价格 | 理由 |
|---|---|---|
| **下载价** | 8 元 | 参考"死了么"原价 1 元 → 调 8 元后登顶付费榜 |
| **订阅** | ❌ 不做 | 中国用户订阅疲劳 + 病耻感 |
| **内购** | v2.0+ | 暂缓 |
| **广告** | ❌ 永远不做 | 病耻感 + 隐私 |
| **数据售卖** | ❌ 永远不做 | 隐私优先 + 法律风险 |

**未来路线（v1.0+）**：
- 医生版（订阅制）：医生看患者依从性
- 加密备份（订阅制）：跨设备同步
- 暂不接广告 — 数据敏感 + 用户脆弱

### 4.2 收入结构（Year 1 保守预测）

| 来源 | 占比 | 金额 |
|---|---|---|
| 下载付费 | 90% | ~720000 元 |
| 公益基金 / 补贴 | 10% | ~80000 元 |
| **合计** | 100% | **~800000 元** |

**收入里程碑**：

| 阶段 | 时期 | 累计下载 | 收入 |
|---|---|---|---|
| v0.5 上架 | Month 1-3 | 1000 | 8000 元 |
| v1.0 完整 | Month 4-6 | 10000 | 80000 元 |
| v1.5 优化 | Month 7-12 | 50000 | 400000 元 |
| v2.0 + 出海 | Year 2 | 200000 | 1600000 元 |

**目标**：1 年内做到 5 万下载，成为"精神心理吃药管理"细分赛道的 No.1。

### 4.3 成本结构

| 项目 | Year 1 成本 | 备注 |
|---|---|---|
| iOS 开发者 | 700 元 | 99$ / 年 |
| Android 开发者 | 175 元 | 25$ 一次性 |
| 阿里云 ECS | 199 元 | 已有 |
| SendGrid | 0 元 | 免费 100 封 / 天 |
| 域名 + SSL | 100 元 | 已有 |
| **总成本** | **~1200 元** | 几乎纯利润 |

### 4.4 公益属性

- **不商业化敏感数据**（情绪 / 吃药记录永不出售）
- **可以免费给低收入患者**（凭学生证 / 低保证）
- **可以接入医院**（医院版永久免费，做口碑）
- **可以接入国家心理援助热线**（替代 mock）

### 4.5 长期愿景

- **Year 1**：5 万下载 = 40 万收入（自费，不融资）
- **Year 2**：20 万下载 + 出海 + 公益基金 = 200 万
- **Year 3**：100 万下载 + B2B（医院 / 咨询师合作）= 1000 万
- **退路**：用户基数起来后，可被大厂收购（参考 Calm / Headspace 模式）

---

## §5 · 核心机制

### 5.1 每日打卡

- 主页 1 个大按钮：「我今天吃了药」
- 点完：haptic 反馈 + 庆祝动画 + streak +1
- 状态记忆：当天已打卡 → 按钮变 "✓ 已完成"

### 5.2 多药物管理

- 首次引导 step 2：设置药物（名称 / 剂量 / 每天时间点）
- 多个 time 点 → 每天 N 次精准推送
- 临时吃药（"今晚加了 1 次"）→ 关联到现有药物，自由输入

### 5.3 失联兜底（48h）

- 漏 2 天（48h）未打卡 → 自动 SMS 通知紧急联系人
- 措辞刻意温和：「请你方便的时候提醒我按时吃药」而非「快不行了」
- 多联系人按 sortOrder 顺序发送，全部失败才放弃

### 5.4 CareEngine 规则引擎

**4 种触发规则**（每天 0:00 跑一次）：

| 规则 | 触发条件 | 动作 |
|---|---|---|
| `secondDayMissed` | 昨天 + 今天都没打卡 | SMS 给联系人 |
| `lateCheckInHabit` | 最近 7 天有 3 天 18:00 后才打卡 | 调整推送时间更早 |
| `weekPerfect` | 连续 7 天按时打卡 | 周日庆祝推送 |
| `none` | 规则不匹配 | 静默 |

`shouldFire()` 3 态：false 不调 / true 调 `showNow` / 抛异常被 try / catch 包。

### 5.5 邮件 / SMS 模板

**邮件模板**（生产中）：

```
主题：来自慢病管家的一个小提醒

你好：

小明 最近两天没在「慢病管家」App 里签到了。
如果你方便的时候，可以轻轻提醒他 / 她一下。

不需要追问发生了什么，也不需要特别紧张。
有时候只是一句"今天吃药了吗"就够了。

——慢病管家
```

**关键设计**：
- 不说"吃药"——只说"签到"（避免邮件被过滤 / 暴露）
- 不说"紧急"——只说"如果你方便的时候"
- 不说具体天数——只说"最近两天"
- 行动可执行："今天吃药了吗"

**SMS 模板**（v0.6+ 接入阿里云，替代 mock）：

```
【慢病管家】您已 2 天未在 App 签到。
如有不便，可联系家人或医生。
本提醒不显示具体病情。
```

**User Story**（验收标准）：

| ID | Story | 验收标准 |
|---|---|---|
| US-01 | 作为患者，我希望每天 20:00 收到打卡提醒 | 推送准时、点击进入主页 |
| US-02 | 作为患者，我吃每颗药 3 秒内能完成打卡 | 1 滑 1 点，不超过 3 秒 |
| US-03 | 作为患者，我漏打卡 48h 后家人收到通知 | 邮件 / SMS 格式正确、无病名、可联系 |
| US-04 | 作为患者，我做 PHQ-9 不到 2 分钟 | 9 题、5 选项、左右滑动切换 |
| US-05 | 作为患者，我能导出 PDF 复诊给医生 | PDF 含吃药率 / 情绪曲线 / 漏药日 |
| US-06 | 作为患者，我的所有数据都在本地 | 无注册、无云端、AES-256 |
| US-07 | 作为患者，我能在状态差时一键拨危机热线 | 主页底部"我现在状态不好"按钮 |
| US-08 | 作为患者，我能看到"心情 × 吃药"洞察 | 双轴图、阈值告警、建议文本 |

---

## §6 · 技术栈与架构

### 6.1 技术栈

| 组件 | 版本 | 选型理由 |
|---|---|---|
| **Flutter** | 3.44.5 stable | 跨 iOS / Android / 未来 watch，UI 性能优 |
| **Dart** | 3.12.2 | Flutter 内置 |
| **状态管理** | flutter_riverpod 3.3.2 | compile-time 安全 + autoDispose + Provider 测试覆盖好 |
| **本地数据库** | drift 2.20.3 | 类型安全 + SQL 表达力 + SQLCipher 加密好接入 |
| **数据库加密** | sqlcipher_flutter_libs 0.6.4 | 行业标准加密 SQLite |
| **路由** | go_router 14.6.1 | 声明式 + 深链 + ShellRoute 适配底部导航 |
| **图表** | fl_chart 0.69 | 纯 Dart，性能好 |
| **本地通知** | flutter_local_notifications 17.2.3 | 跨平台时区感知 |
| **PDF 报告** | pdf 3.11 + printing 5.13 | 离线生成 PDF |
| **加密** | flutter_secure_storage + encrypt (AES-256) | iOS Keychain / Android Keystore + 自定义加密 |
| **录音** | record 5.2.0 + audioplayers 6.1.0 | 树洞语音用 |
| **国产 ROM 兼容** | permission_handler 11.3.1 | 通知 / 电池优化权限 |
| **邮件 / SMS** | SendGrid / 阿里云占位 | 国内需代理；已配 Mock 模式 |

### 6.2 4 层架构 + 共享层（**当前**）

```
lib/
├── main.dart              # 入口（含通知 init / DB key 解锁）
├── app.dart               # App 根 + ProviderScope + 跨 midnight refresh
├── core/                  # 跨 feature 共享（v0.17 round 8 后）
│   ├── data/             # 数据库 + 仓库 + service
│   ├── theme/            # AppTokens + M3 主题
│   ├── l10n/             # 静态 strings（domain 层 fallback）
│   ├── shared/           # formatters / json_codec / mood_visual / DomainValue
│   ├── routing/          # go_router 配置
│   └── l10n_flutter/     # flutter_localizations（UI 层）
├── domain/                # 领域层（**0 Flutter / 0 Drift / 0 data 依赖**）
│   ├── entities/         # 业务实体（*Entity 后缀）
│   ├── logic/            # 业务规则（量表 / streak / care engine / 报告）
│   ├── repositories/     # 抽象接口（无实现）
│   └── usecases/         # 用例（业务编排）
└── presentation/          # UI 层
    ├── providers/         # Riverpod providers（按职责 3 文件拆）
    ├── pages/             # 1 个目录 = 1 个页面（按 feature 拆 8 个）
    └── widgets/           # 通用组件
```

**依赖方向（强制）**：
- `presentation → domain ← data`（domain 是接口所有者）
- `core/shared` 被 3 层都能用（但被 `check_all` 监测：只被 1 层用 = 移走）

### 6.3 feature-first 重构（v0.17 round 8 进行中）

**目标**：把 layer-first 改成 feature-first，让每个 feature 自洽。

**新结构**：

```
lib/
├── main.dart
├── app.dart
├── core/                  # 跨 feature 共享
│   ├── database/         # ← lib/data/database/
│   ├── theme/            # ← lib/theme/
│   ├── l10n/             # ← lib/l10n/
│   ├── shared/           # ← lib/shared/
│   ├── routing/          # ← lib/routing/
│   └── services/         # 跨 feature service（notification / email / crypto）
└── features/             # 8 个 feature
    ├── home/             # 主页（打卡 / 庆祝 / mood / vent 入口）
    ├── setup/            # 首次设置（consent / welcome / medication / done）
    ├── settings/         # 设置
    ├── trend/            # 趋势（list + calendar 视图）
    ├── assessment/       # 心理评估（答题 + 历史 + 提醒 section）
    ├── medication/       # 用药（calendar / refill / today / report）
    ├── vent/             # 树洞（list / compose / detail）
    └── check_in/         # 打卡按钮
```

**跨 feature 共享规则**：
- 跨 feature 的 entity（如 `MedicationEntity` 被 check_in / medication / trend 复用）→ 放 owner feature，其他 feature 调 owner 的 public API
- 跨 feature 的 service → 放 `core/services/`
- 4 层纯度检查**仍然生效**（feature 内仍 3 层 + 跨 feature 不允许 cyclic）

**当前状态**：
- 8a ✅ 共享层（database / theme / l10n / shared / routing）移到 `lib/core/`
- 8b ⏳ 8 个 feature 切分 — 进程到一半重置回 4 层架构（**见 §11 路线图**）
- 8c ⏳ check_all 脚本重写（监测 `core/` + `features/`）

### 6.4 架构检查（CI 友好）

```bash
dart scripts/check_all.dart   # 一次出 2 份报告
```

- **[1/2] 4 层架构纯度**：domain 0 flutter / 0 drift / 0 data / 0 presentation
- **[2/2] 架构语义一致性**：domain `*Entity` ↔ drift `@DataClassName('X')` 一一对应 + shared 工具使用度

违规时 exit 1，CI 会 fail。

**注**：`dart run` 在本项目会触发 `objective_c` build hook 失败，用 `dart` 直接跑。

### 6.5 数据模型

**Drift 8 表**（schemaVersion 11，v0.15 树洞新增 `vent_entries`，多次 schema 升级均带 migration）：

| 表 | 用途 | 关键字段 |
|---|---|---|
| `user_profiles` | 用户档案 | name, created_at, language |
| `medications` | 药物 | name, dosage, times (JSON 数组), start_date, end_date, refill_date, refill_remind_days |
| `check_ins` | 每日打卡 | timestamp, mood_score (1-10), note, sleep_hours, exercise_minutes |
| `mood_entries` | 情绪日记 | timestamp, score (1-10), note, factors (JSON) |
| `contacts` | 紧急联系人 | name, phone, email, sort_order, is_archived |
| `vent_entries` | 树洞 | timestamp, text, audio_path, audio_duration_ms, content_type |
| `report_histories` | 报告历史 | generated_at, type, file_path, parameters (JSON) |
| `assessments` | 心理评估（PHQ-9 / GAD-7）| type, answers (JSON), total_score, severity |

**Domain Entity 命名**：
- `UserProfileEntity`、`MedicationEntity`、`CheckInEntity`、`MoodEntryEntity`、`ContactEntity`、`VentEntryEntity`
- **`Entity` 后缀强制**：避免跟 drift `@DataClassName('X')` 冲突

**数据流**：

```
[Presentation] ── ref.read(provider) ──> [Repository] ── watch / insert / delete ──> [Drift]
        ▲                                                                       │
        │                                                                       │
        └────────────── Stream<*> ◀── watchAll / getById ◀─────────────────────┘
```

### 6.6 关键业务流程

#### 6.6.1 首次引导（setup）

1. 欢迎页 + 隐私说明
2. 个人信息（姓名 + 紧急联系人）
3. 药物设置（名称 + 剂量 + 时间点，可多选预设模板）
4. 评估建议（PHQ-9 baseline，可跳过）
5. 通知权限申请 + 引导到系统设置

#### 6.6.2 每日打卡（check_in）

1. 用户点「我今天吃了药」按钮
2. 写 `CheckInEntity`（useCase: `RecordCheckInUseCase`）
3. 写 DB → 触发 `streakSummaryProvider` 自动重算
4. UI 显示庆祝动画 + 鼓励文案（按 streak 天数切换）

#### 6.6.3 失联通知

- 每天 check 时：计算自上次打卡距今小时数
- 超过 48h → 走多联系人循环（按 sortOrder），每个发 SMS
- **措辞固定模板**（"请你方便的时候提醒我按时吃药"），不暴露病情 / 不渲染病情

#### 6.6.4 跨 midnight streak 刷新

- 主页 `streakSummaryProvider` 跨 23:59:59 后不自动失效
- AppRoot 挂 midnight timer，**00:00:05 自动 `ref.invalidate(streakSummaryProvider)`**
- `nextMidnightRefresh(now)` 抽 top-level 纯函数（跨月 / 跨年 / buffer 5s 防 race）
- v0.21 P0-4 加 `crossedMidnightSince(lastCheck, now)` + AppLifecycle 监听，**应对手动改时间 / 跨时区**

---

## §7 · 设计系统

### 7.1 设计哲学

- **温柔克制**（不是科技炫酷）
- **3 秒可达**（任何操作 3 步内完成）
- **不强调成功**（不显示"100 天连续"大字，改用"今天也记得了"小字）
- **不强调失败**（断 1 天不警告，改用"明天再继续"）
- **不刺眼色调**（暖沙色 + 纸白背景，避开高饱和）

### 7.2 设计 Token（`lib/core/theme/app_tokens.dart`）

| 类别 | token | 例子 |
|---|---|---|
| spacing | `spaceXs` / `spaceSm` / `spaceMd` / `spaceLg` / `spaceXl` | 4 / 8 / 12 / 16 / 24 |
| radius | `radiusSm` / `radiusMd` / `radiusLg` | 4 / 8 / 12 |
| fontSize | `fontSizeCaption` / `body` / `title` / `display` | 12 / 14 / 18 / 24 |
| color | `warning` / `warningStrong` / `success` / `error` | ARGB int |
| duration | `durFast` / `durNormal` / `durSlow` | 100 / 200 / 300 ms |
| **curve**（v0.17 round 1）| `curveStandard` / `curveDecelerate` / `curveAccelerate` / `curveDelight` | easeOutCubic / easeOutQuart / easeInCubic / elasticOut |
| breakpoint | `wideScreenMinWidth` | 720 |

### 7.3 色彩（设计稿规范）

| 名称 | 色值 | 用途 |
|---|---|---|
| **主色 · 暖沙色** | #C8A882 | 主按钮、强调（不刺眼）|
| **背景 · 纸白** | #FAF8F5 | 全局背景（不冷）|
| **文字 · 墨黑** | #2C2C2C | 主要文字 |
| **副文字 · 烟灰** | #6B6B6B | 次要文字 |
| **心情 · 绿** | #7FB069 | 🟢 好 |
| **心情 · 黄** | #E6B800 | 🟡 一般 |
| **心情 · 红** | #D17575 | 🔴 差 |
| **警告 · 暖橘** | #E68A4D | 通知、提醒（不用红色）|

### 7.4 字体

- **中文**：思源黑体（Source Han Sans / Noto Sans CJK SC）
- **英文**：Inter
- **标题**：28pt / 600
- **正文**：16pt / 400
- **辅助**：14pt / 400

### 7.5 间距

| Token | 值 | 用途 |
|---|---|---|
| `space-1` | 4px | 紧凑 |
| `space-2` | 8px | 元素内边距 |
| `space-3` | 16px | 卡片间距 |
| `space-4` | 24px | 区域间距 |
| `space-5` | 32px | 大区块 |

### 7.6 圆角

| Token | 值 | 用途 |
|---|---|---|
| `radius-sm` | 8px | 按钮、输入框 |
| `radius-md` | 12px | 卡片 |
| `radius-lg` | 16px | 弹窗、底部 sheet |
| `radius-full` | 999px | 头像、chip |

### 7.7 emil 动效频度决策框架（v0.17 round 1）

| 频度 | 例子 | 动效 |
|---|---|---|
| **100+ / day** | 键盘快捷键、命令面板、核心导航 | **无动画**（Raycast 没 open / close） |
| **Tens / day** | hover 状态、列表导航、频繁 toggle | 微弱（fast + subtle） |
| **Occasional** | modal、drawer、toast、settings | 标准（标准 duration + curveStandard） |
| **Rare / 首次** | onboarding、空状态、success、celebration | 可加 delight（curveDelight） |

**哲学**：高频交互 → 无动画；低频 + 高情绪 → 可以加 delight。

### 7.8 路由转场（v0.17 round 2）

| 类型 | 用法 | 时长 |
|---|---|---|
| `_fadePage` | 主导航（/、/settings） | durNormal |
| `_slideRightPage` | 子页（/settings / reminders） | durNormal |
| `_slideUpPage` | 全屏深页（/setup） | durSlow |

### 7.9 关键交互模式

#### 主页打卡卡片

```
┌─────────────────────────────────┐
│  奥氮平 5mg                     │
│  20:00                          │
│                                 │
│  现在心情如何？                  │
│  🟢      🟡      🔴              │
│ (好)   (一般)   (差)            │
│                                 │
│  [ ✓ 打卡 ]                     │
└─────────────────────────────────┘
```

#### 连续天数

- 不显示大数字，改显示"今天也记得了"
- Streak 数字用小字 + 灰底，不抢眼

#### 紧急联系人状态

```
紧急联系人：小明（妈妈）
最后通知：未通知 / 3 天前（您断药时）
[管理紧急联系人]
```

### 7.10 设计原则

- **Material 3** + `InkSparkle.splashFactory`（v0.17 round 7 A5）
- **温柔色调**：避免高饱和 / 红绿对比，适配抑郁 / 焦虑用户敏感
- **大字体**：body 14-16 sp（默认 Material 太小）
- **haptic 反馈**：打卡完成、删除等关键操作
- **庆祝克制**：streak 数字用 `TweenAnimationBuilder` 0→N 慢慢增长，不用粒子效果

---

## §8 · 通知与提醒系统

### 8.1 通知 id 分段（v0.16 round 19B 修）

| 段 | 用途 | 公式 | cancel 范围 |
|---|---|---|---|
| 1001 | 每天 20:00 通用打卡提醒 | 固定 | 单 id |
| 2000+ | 药物时间点 | `2000 + medId * 10 + timeIdx` | `[2000, 202000)` |
| 3000 | 10am 软提醒 | 固定 | 单 id |
| 4000+ | CareEngine 关怀 | `4000 + ruleIdx` | `[4000, 202000)` |
| 5000+ | 续方 | `5000 + medId` | `[5000, 202000)` |
| 6000+ | 续方提醒（v0.19B） | `6000 + medId` | `[6000, 202000)` |
| 99001 | 测试通知 | 固定 | 单 id |

**统一 cancel range 200000**：covers medId 几万个，远超实际用户量。

### 8.2 国产 ROM 静默杀后台通知（v0.16 round 20 修）

**问题**：小米 / 华为 / OPPO / Vivo / 魅族 默认禁止 App 后台运行 + 自启动 + 精确闹钟。**用户报"到点没收到提醒"99% 是这个原因。**

**修法**：
- 设置页加 `NotificationStatusCard` 自检卡
  - 状态显示：当前已排队的待发通知数（0 / N / 不支持三态）
  - **测试通知按钮**：点一下立即推一条
  - **查看已排队通知**：弹 dialog 列出所有 `pendingNotificationRequests` 标题
  - **国产手机后台引导**：折叠面板展开 5 大品牌每家 2-3 步
  - `kIsWeb` fallback：web 端显示"通知功能仅在 Android / iOS 可用"

**debug 误区**：不要靠 `developer.log` 排查（用户看不到），先检查自检卡 pendingCount。

---

## §9 · 树洞（Vent / 私密倾诉空间）

**v0.15 round 18 上线**。**完全独立模块**。

### 9.1 隐私边界（**强制**）

| 模块 | 允许 | 禁止 |
|---|---|---|
| 树洞 | 无 | 趋势 / 评估 / CareEngine / SafetyWatch / 通知 / 关怀 |

**如果发现树洞内容进了趋势页 = bug，立即修。**

### 9.2 设计原则

- 文字 / 语音 / 混排三种记录形式
- 录音用 `record` 5.2.0，播放用 `audioplayers` 6.8.1
- audio 文件存 `app docs/vent_audio/`（DB 路径引用，DB 整体 SQLCipher 加密）
- 主页加"倾诉 🌲"入口按钮
- 列表 + 长按删除；详情 + 进度条

**为什么独立**：保护"私密空间"信任 → 即使内容含"想死"也不通知家人。

### 9.3 命名

- domain entity: `VentEntryEntity`
- drift table: `@DataClassName('VentEntry')`（避免冲突）

---

## §10 · 测试策略

### 10.1 三层测试

| 层 | 类型 | 工具 | 速度 | 覆盖目标 |
|---|---|---|---|---|
| **domain** | 纯 Dart 单测 | `test/domain/*` | ⚡⚡⚡ < 1s | 90%+ |
| **data** | Drift round-trip | `test/data/*` | ⚡⚡ ~5s | 80%+ |
| **presentation** | widget test | `test/presentation/*` | ⚡ ~20s | 70%+ |

**当前**：703 cases pass（v0.17 round 7 后 + v0.18 增补）。

### 10.2 覆盖分布

| 模块 | 覆盖 | 备注 |
|---|---|---|
| domain entities | 100% | 业务方法 + 边界 |
| domain logic | 90%+ | streak_calculator / care_engine / scale / medication_report |
| data repositories | 80%+ | round-trip + 边界 |
| presentation widget | 70%+ | 主要页面 + 交互 |
| **safety_watch / care_engine / schedule service 单独** | 偏低 ⚠️ | 后续 round 9 补 |

### 10.3 TDD 纪律

- v0.16 几轮 bug 修都是"先发现 → 再写 test"，**不是红灯先行**
- v0.17 round 7 B4 demo：先写 `calendar_window_provider_test.dart` 引导 `CalendarWindowNotifier` 实现
- **下次新功能试试先写 test**（C1 / C3 / B2 候选场景）

---

## §11 · 路线图

### 11.1 当前进度

| 版本 | round | 内容 | 状态 |
|---|---|---|---|
| v0.5 | 1-2 | 极简 MVP（主页打卡 + 设置 + 引导） | ✅ |
| v0.6 | - | 多联系人邮件 + 失联检测 v0 | ✅ |
| v0.7 | - | SMS 抽象 + CareEngine + 10 规则 | ✅ |
| v0.8 | - | PHQ-9 + 量表历史 + Web 加载 | ✅ |
| v0.12 | - | 邮件 / SMS 通知 + 趋势页 + 临时吃药 | ✅ |
| v0.13 | - | 多档案 + 续方提前 + 评估历史 sparkline | ✅ |
| v0.14 | - | 续方管理 + 评估历史独立页 + 用药日历 + 提醒中心 | ✅ |
| v0.15 | round 18 | 树洞（完全独立） | ✅ |
| v0.16 | round 1-20 | 4 层架构 + 5 轮 code review + 9 个 gotcha fix + 通知自检 | ✅ |
| v0.17 | round 1-7 | emil 动效 + Riverpod 3.0 升级 + 跨 midnight + 7 项 process 改进 | ✅ |

**当前**：v0.17 round 7 已 commit，**703 test pass**，4 个 info-level `prefer_const_constructors` 待清。

### 11.2 下一步（v0.17 round 8+）

| 优先级 | 项 | 描述 | 工作量 |
|---|---|---|---|
| 🔴 P0 | **Feature-first 重构（8b）** | 把 layer-first 切到 feature-first（8 个 feature） | 大（3-5 commit） |
| 🔴 P0 | **check_all 脚本重写（8c）** | 监测 `core/` + `features/` 新结构 | 中（1 commit） |
| 🟡 P1 | **C1 自动 retry + 指数退避** | DB / notification 失败自动重试 | 中（1 commit） |
| 🟡 P1 | **C3 Stream `==` 过滤** | 之前是 `identical`，减少重复 rebuild | 小（1 commit） |
| 🟢 P2 | **B2 拆 Notifier** | setup 22 处 setState + medication_calendar / vent 拆 Notifier | 中（1-2 commit） |
| 🟢 P2 | **B3 ConsumerStatefulWidget → ConsumerWidget** | 9 个 widget 状态提到 provider | 中（1-2 commit） |
| 🟢 P2 | **升 Drift 2.20.3 → 2.34.2** | drift 实际没 3.x，最新 2.34.2。**注**：drift_dev 2.34.0 跟 sqlparser 0.44.6 撤回冲突，需 pin `sqlparser: 0.44.5` 在 `pubspec_overrides.yaml` | 小（1 commit） |
| 🟢 P2 | **docs sync** | CHANGELOG + AGENTS.md + Memory 同步 v0.17 round 8 | 小（1 commit） |
| ⚪ P3 | B5 测试覆盖补 | safety_watch / care_engine / schedule service 单独补 | 中 |
| ⚪ P3 | C4 统一 Notifier API | 项目 0 个用到，skip | - |

### 11.3 远期（v1.0+）

- 加密云端备份（订阅制）
- 医生版（订阅制）
- Apple Watch 伴侣 App
- i18n 英文版（出海）
- 接入国家心理援助热线（替代 mock）
- AI 情绪记忆（v2.0+）

---

## §12 · 团队分工

### 12.1 角色 + 职责

| 角色 | 主要职责 | 不应做 |
|---|---|---|
| **PM**（项目负责人）| 路线图优先级、用户访谈、版本发布节奏、AGENTS.md 维护 | 不写 production 代码（review 可）|
| **产品设计（Designer · 外包）** | 视觉稿、动效、design token、文案 | 不写业务逻辑代码 |
| **全栈 Flutter Dev**（项目负责人）| feature 实现、bug 修、单元测试、SQL schema、CI 脚本 | - |
| **架构师**（= 全栈 Dev 自己）| 4 层架构纯度、check_all 脚本、Riverpod 模式、Drift 升级 | - |
| **QA**（兼职 / 后续轮次）| 黑盒测试、国产 ROM 兼容性、用户场景复现 | 不写 production 代码（可写 test）|
| **DevOps**（= 全栈 Dev 自己）| CI / CD、构建、发布 | - |

### 12.2 当前最小团队配置

实际是 **单人项目**（PM + 全栈 Flutter Dev = 项目负责人）：

- **1 全栈**（PM + Flutter Dev + DevOps）
- **1 兼职设计**（Fiverr / 99designs，约 5000 元 / 月）
- **1 兼职 QA**（朋友，约 3000 元 / 月）

总人力成本：~10000 元 / 月

### 12.3 沟通规范

- **commit message 用纯英文**（PowerShell 路径解析限制，中文标点会被吞）
- `AGENTS.md` 给 AI Agent 看（不是人看，但人是 source of truth）
- `CHANGELOG` 跟代码同步更新（每个 commit 前 / 后）
- 文档目录：`whitePaper/` = 项目白皮书 / `docs/` = 设计 / 部署 / 邮件 / `AGENTS.md` = 给 AI

---

## §13 · 风险与缓解

### 13.1 已知风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 国产 ROM 静默杀通知 | 用户收不到提醒 | NotificationStatusCard 自检 + OEM 引导 |
| 隐私泄露 | 患者敏感数据 | SQLCipher + 零云端 + 树洞独立 |
| 自伤内容被 CareEngine 误触发 | 通知家人造成二次伤害 | CareEngine 不知道树洞（隐私边界）|
| 漏服失联 | 病情波动 | 48h 兜底 + 措辞温和 |
| 患者擅自停药 | 停药综合征 | 用药教育 + 续方提醒（不强制）|
| Drift 升级断路 | 不能跟上游 | 锁 `pubspec_overrides.yaml` 临时绕道（v0.17 round 8 已 pin sqlparser 0.44.5）|
| 单 Dev 单点 | 关键人风险 | 文档化（AGENTS.md / 本白皮书）+ TDD（v0.17 round 7 B4）|
| App Store 审核拒绝（"死"字眼、医疗类）| 上架失败 | 不出现"死 / 危"字眼；定位为"工具"非"医疗" |
| 复诊医生对 PDF 报告不感兴趣 | 报告功能白做 | 备用：可导入 Apple Health / Google Fit |
| 病耻感导致用户不敢下载 | 高 | 营销低调 + 口碑传播 + App 名字中性 |
| SendGrid 国内送达率低 | 邮件兜底失效 | 改 Resend / Postmark / 国内邮件商 |
| 复发预警算法误报 | 触发频繁 | 阈值保守（连续 3 天低 + 吃药率 < 50%）|

### 13.2 硬约束（不可妥协）

- ❌ 不出现"死 / 危"字眼（App Store + 病耻感）
- ❌ 不强制注册 / 不强制手机号（隐私）
- ❌ 不做游戏化（精神心理用户）
- ❌ 不暴露病名（邮件 / 通知 / 营销）
- ❌ 不售卖用户数据（法律 + 道德）
- ❌ 不做订阅（中国用户疲劳 + 病耻感）
- ❌ 不做社交（病耻感 + 暴露身份）
- ❌ 不做 AI 倾听（已红海 + 责任大）

### 13.3 软约束（可妥协但慎重）

- ⚠️ 暂不接医院（避免责任）
- ⚠️ 暂不做小程序（用户群不爱用）
- ⚠️ 暂不做 Android 平板 / iPad 适配
- ⚠️ 暂不做 Wear OS / Apple Watch（v2.0+）

### 13.4 失败信号（按 plan 里的 Phase 4 约定）

| 信号 | 阈值 | 动作 |
|---|---|---|
| 邮件送达率 | < 95% | 换邮件服务商 |
| 紧急联系人不填 | > 30% | 加引导动画 + 文案优化 |
| App Store 拒绝 | > 2 次 | 改名字 + 重新定位 |
| 1 月内下载 | < 100 | 暂停 v1.0 开发，回访用户 |
| 评分 | < 3.5 | 紧急回访 + 复盘 |

### 13.5 监管风险

- **《个人信息保护法》（PIPL）**：纯本地 + 不强制注册 + 不上传数据 = 完全合规
- **《互联网医疗管理办法》**：定位"工具"不"医疗" + 不出诊断建议 = 避开
- **App Store 医疗类审核**：不出现"治疗 / 治愈 / 诊断"字眼 + 附危机热线
- **Google Play Health 类审核**：同上

---

## §14 · CI / CD 与本地验证

### 14.1 本地验证

```bash
flutter analyze    # 必须 0 error / 0 warning（info-level 可）
flutter test       # 必须 703 cases 全过
dart scripts/check_all.dart  # 4 层架构纯度 + 一致性
python scripts/check_cross_feature.py  # 跨 feature import 检查
```

### 14.2 打包

```bash
# Web（H5）
flutter build web
# 输出：build/web/
# dev：python -m http.server 8358（drift worker 404 → production 模式）

# Android APK
flutter build apk --release
# 输出：build/app/outputs/flutter-apk/app-release.apk

# iOS（macOS only）
flutter build ios --release
# 输出：ios/Runner.xcarchive
```

### 14.3 部署目标

- **Web**：PWA + 静态托管
- **Android**：Google Play + 国产应用市场（华为 / 小米 / OPPO / Vivo / 魅族）
- **iOS**：App Store

---

## §15 · 已知坑（Gotchas — 必读）

> 这些是 v0.16 - v0.17 期间踩过的坑，新人先看这一节。完整索引见 `AGENTS.md` "已知坑"段。

| # | 坑 | 原因 | 修法 |
|---|---|---|---|
| 1 | `flutter_secure_storage` Android 首次启动 ~200ms 延迟 | 设备 Keystore 启动慢 | 提前 init + Loading state |
| 2 | Drift schema 升级漏 migration | `app_database.dart` schemaVersion++ 但忘加 `onUpgrade` | PR review + 必看 migration 段 |
| 3 | `VentEntryEntity` vs `VentEntry` 命名冲突 | domain entity vs drift @DataClassName | domain 强制 `*Entity` 后缀 |
| 4 | `audioplayers + record` 一起用文件锁冲突 | 同进程两个 handle | recorder 先 dispose 再 player |
| 5 | `Stream subscription leak` | `_player.onXxx.listen()` 没存 subscription | 存字段 + dispose 时 cancel |
| 6 | `BuildContext` 跨 async gap 警告 | analyzer `use_build_context_synchronously` | mounted check 或 `this.context` |
| 7 | `DateTime.now()` 多次 race | 跨 midnight 边界日期不一致 | 函数入口 `final now = DateTime.now();` 一次 |
| 8 | **Notification id cancel range 公式不匹配** | 之前 1000 / 100000 范围太窄 | 统一 200000 + id 公式要对应 |
| 9 | **Filename collision** | 同毫秒内 2 个文件覆盖 | timestamp + 4 位 random suffix |
| 10 | `setState` after defunct | listener 内调 setState + State dispose 后触发 | listener 内 `if (!mounted) return;` guard |
| 11 | **Resource acquire / release** 必须 try / finally | try 块内 acquire 资源 + dispose 一气呵成，异常时 dispose 不跑 | `final x = ...; try { use } finally { x.dispose(); }` |
| 12 | **`.first` / `.last` 隐式排序假设** | 依赖 drift orderBy 隐式顺序，future caller 传 unsorted 数据会算错 | 函数内部显式 sort + unsorted input regression test |
| 13 | **GoRouter path param unsafe parse** | `int.parse` 抛 FormatException → app 崩 | `int.tryParse(...) ?? 0` |
| 14 | **国产 ROM 静默杀后台通知** | 小米 / 华为 / OPPO / Vivo / 魅族默认禁止后台 + 自启动 | NotificationStatusCard 自检 + OEM 引导 |
| 15 | **Riverpod 3.x `valueOrNull` → `value`** | 2.6 的 `AsyncValue.valueOrNull` 在 3.x 改名 | 全局搜索 `valueOrNull` 替换 |
| 16 | **Riverpod 3.x `ref.mounted` 仅限 Notifier** | Provider / StreamProvider / ConsumerStatefulWidget 不能用 | 保持 `!mounted` check（项目 27 处）|
| 17 | **Drift sqlparser 0.44.6 撤回** | 0.44.6 被作者撤回，build_runner AOT kernel 编译失败 | `pubspec_overrides.yaml` pin `sqlparser: 0.44.5` |
| 18 | **跨 midnight streak 不刷新** | streakSummaryProvider 自身没监听时间变化 | AppRoot 挂 midnight timer + `nextMidnightRefresh` 纯函数 |
| 19 | **emil 动效 token 缺 curve** | 之前 app_tokens.dart 只有 dur* 缺 curve | 4 个 curve token + emil 决策框架 doc 注释 |
| 20 | **go_router 默认无 page transition** | GoRoute.builder 默认切换无动画 | `pageBuilder` + `CustomTransitionPage`（3 类 transition）|
| 21 | **Flutter widget test ink_sparkle shader 缺失** | Material 3 InkWell 需要 `shaders/ink_sparkle.frag` | 从 SDK 复制到 `assets/shaders/` + `pubspec.yaml: shaders:` 字段 |
| 22 | **跨 feature import**（v0.17 round 12）| presentation/ 按 feature 拆后跨 feature 耦合 | `python scripts/check_cross_feature.py` 守门 |
| 23 | **DateTime.toIso8601String() 缺时区后缀** | 没 `Z` 后缀时 DateTime.parse() 当 local 解析 | 统一 `.toUtc().toIso8601String()` + `_isoUtc()` helper |
| 24 | **AppRoot 定时器 / DST** | 跨时区 / 手动改时间后 timer 失效 | `crossedMidnightSince()` + AppLifecycle 监听 + `dayChangeTickProvider` |

---

## §16 · 决策记录（Decision Log）

| 决策 | 原因 |
|---|---|
| 4 层架构 | domain 易测试 + 易复用 + 0 Flutter 依赖 |
| SQLCipher + 零云端 | 精神心理患者数据敏感 |
| 树洞独立表 | 隐私边界：绝对不进任何分析 / 通知 / 关怀 |
| Audio 存本地文件 | DB 体积不能爆炸，文件用路径引用 |
| `ProviderScope` overrides 测试 | 真实 DB 测试太慢，in-memory + override 足够覆盖 |
| 主页底部按钮加"倾诉" | 用户主要路径 = 打卡 / 设置 / 倾诉 3 个核心动作 |
| `RadioListTile` → `RadioGroup`（Flutter 3.32+） | 弃用警告 |
| `try / finally` 资源释放 | 异常路径也要 release |
| `int.tryParse` 替代 `int.parse` | GoRouter 路径参数 fallback |
| `reduce(isAfter)` 替代 `.last` | 不依赖 list 顺序 |
| commit message 用纯英文 | PowerShell 路径解析限制 |
| 8 元一次性买断，无订阅 | 用户人群 + 商业模式 |
| emil 动效频度决策 | 100+ / day 无，tens 微，occasional 标准，rare 可 delight |
| **feature-first 重构（v0.17 round 8）** | 拆 lib/ 按 feature，让每个 feature 自洽 |
| `dayChangeTickProvider` (v0.21 P0-6) | 跨 midnight / 手动改时间 / DST 都能触发 widget rebuild |
| `crossedMidnightSince()` + AppLifecycle (v0.21 P0-4) | 替代单一 timer，应对挂后台 / 系统时间变 |
| 通知 id 公式 + cancel range 统一 200000 | covers medId 几万个，远超实际 |
| `isoUtc()` helper 统一 UTC 后缀 | drift 自己处理 UTC，JSON 序列化要 `.toUtc().toIso8601String()` |
| v0.21 P0-7 god class facade 评估 | notification_service 25KB 已是 facade（4 sub-service + 自己 ID 公式 + 自检卡） |

---

## §17 · 附录

### 17.1 关键文档

- `README.md` — 项目简介（公开版）
- `AGENTS.md` — AI Agent 视角的项目指引（私有）
- `docs/CHANGELOG.md` — 版本变更（Keep a Changelog 格式）
- `docs/DEPLOYMENT.md` — 部署相关
- `docs/SENDGRID_SETUP.md` — 邮件服务配置
- `whitePaper/慢病管家-白皮书-v3.0.md` — **本文件**（团队一站式档案，v3.0）
- `whitePaper/archive/` — 归档的历史白皮书 + 不相关材料

### 17.2 关键脚本

- `scripts/check_all.dart` — 4 层架构纯度 + 一致性
- `scripts/check_cross_feature.py` — 跨 feature import 检查
- `scripts/test_delivery_rate.dart` — 通知送达率测试
- `scripts/8a2_rewrite_to_absolute.py` — 把所有相对 import 转 package: 绝对路径（v0.17 round 8）
- `scripts/8a_rewrite_imports.py` — 旧版本（v0.17 round 8 初期，已被 8a2 替代）

### 17.3 关键命令

```bash
# 跑
flutter run -d <device>

# 监听代码生成
dart run build_runner watch --delete-conflicting-outputs

# 单文件测试
flutter test test/domain/streak_calculator_test.dart

# 4 层架构检查
dart scripts/check_all.dart

# 跨 feature import 检查
python scripts/check_cross_feature.py

# 解决 Drift sqlparser 撤回
# 已在 pubspec_overrides.yaml pin sqlparser: 0.44.5，dart pub get 自动应用
```

### 17.4 关键文件路径

| 文件 | 用途 |
|---|---|
| `lib/main.dart` | 启动顺序、SQLCipher 初始化、通知 init |
| `lib/app.dart` | App 根 + ProviderScope + 跨 midnight refresh |
| `lib/core/data/database/app_database.dart` | schemaVersion 11，所有表 + migration |
| `lib/domain/logic/care_engine.dart` | 失联检测 / 续方 / 通知触发核心规则 |
| `lib/core/data/services/notification_service.dart` | 通知 id 公式 + cancel range + 自检卡依赖 |
| `lib/presentation/providers/core_providers.dart` | 全局 provider 注册表 |
| `lib/core/routing/app_router.dart` | 所有页面路由 + shell (NavigationRail) |
| `lib/core/theme/app_tokens.dart` | 设计 Token（spacing / radius / fontSize / color / duration / curve / breakpoint）|

### 17.5 紧急联系方式（产品设计）

- **北京心理危机研究与干预中心**：010-82951332
- **全国心理援助热线**：400-161-9995
- **生命热线**：400-821-1215

### 17.6 关键参考资料

1. **PHQ-9**（抑郁筛查量表）：Kroenke et al., 2001
2. **GAD-7**（焦虑筛查量表）：Spitzer et al., 2006
3. **死了么 / 在么在么**：竞品，验证了模式
4. **Medisafe / Pillo**：竞品，多药管理参考
5. **Woebot**：竞品，CBT 循证参考
6. **心光 App**：竞品，心情红绿灯思路来源
7. **Duolingo**：竞品，习惯养成机制参考

---

## 文档维护

- **owner**：项目负责人（详细见 git log 活跃 commit 作者）
- **更新时机**：每个 round commit 后 / 每个版本发布前
- **下次 review**：v0.17 round 8 完成时（feature-first 重构 + check_all 重写）
- **变更原则**：所有数字、路线、决策以本文件为准；正文细节可查但不轻易改基线

## 变更日志

| 版本 | 日期 | 变更 |
|---|---|---|
| v3.0 | 2026-07-20 | **合并** `WHITEPAPER.md`（v0.17 round 7 后项目档案）+ `WHITE_PAPER.md`（v0.1 产品白皮书），去重 + 修正过时数据；移除 `慢病管家产品白皮书-v2.0.md`（与 v0.1 几乎重复）|
| v2.0 | 2026-07-17 | 整合多份白皮书，统一为团队执行版（**内容已与 v0.1 几乎一致，无新增**，v3.0 合并后弃用）|
| v0.1 | 2026-07-17 | 首版（v0.5 MVP 完成后）|
| 项目档案 v0.17 round 7 | 2026-07-17 | 19 节项目档案，配套本白皮书使用（v3.0 合并后弃用独立版）|
