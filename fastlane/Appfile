# v0.27 round 67 (Sprint 1 上架前 P0, appstore C-P0-11): Appfile
#
# fastlane 用, 4 个值上 store 前**必须**替换为真实值。
# 真实值从 Apple Developer 后台拿:
#   - apple_id: App Store Connect 登录邮箱
#   - team_id: Apple Developer 后台 → Membership → Team ID
#   - itc_team_id: App Store Connect → Users and Access → Keys → (选 org)
#
# v0.30 R96 (2026-08-07): 改 ENV 模式软隐藏, 真实凭据不进 git
# (跟 .env / key.properties 同一策略, .env 已在 .gitignore 排除)
#
# **使用方式**:
# 1. cp .env.example .env
# 2. 在 .env 填真实值 (APPLE_ID / TEAM_ID / ITC_TEAM_ID)
# 3. fastlane 会自动读 .env (需 dotenv gem, fastlane 默认带)
#
# **替代方案** (推荐, 更安全):
# 用 API Key 模式替代 apple_id + team_id, 加到 Fastfile:
#     app_store_connect_api_key(
#       key_id: ENV["APPLE_KEY_ID"],
#       issuer_id: ENV["APPLE_ISSUER_ID"],
#       key_filepath: ENV["APPLE_KEY_FILEPATH"],
#     )
# API Key 模式不依赖 apple_id 登录, 更适合 CI/CD

app_identifier(ENV["APP_IDENTIFIER"] || "com.chroniccare.chroniccare")
apple_id(ENV["APPLE_ID"])
team_id(ENV["TEAM_ID"])
itc_team_id(ENV["ITC_TEAM_ID"])
